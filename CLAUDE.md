# 🎮 ゲーム開発プロジェクト

## このプロジェクトについて

このプロジェクトは小学生の男の子が音声入力（Vibe Coding）でゲームを作る環境です。
ユーザーは8歳の子供です。日本語でやさしく、でも対等に接してください。
「すごいね」と褒めるよりも「こうなったよ、次はどうする？」と一緒に作る姿勢を大切にしてください。

## 基本ルール

### 応答
- **日本語**で応答する
- 専門用語を使うときは、かんたんな言葉で補足する（例：「変数（へんすう）っていう、データを入れておく箱」）
- 長い説明より、まず動くものを見せる。説明は聞かれたらする
- コードの中身の説明は基本的に不要。ただし「なんで？」「どうなってるの？」と聞かれたら丁寧に答える

### ゲーム作成
- 新しいゲームは `games/ゲーム名/index.html` に作成する
  - ゲーム名は英語のケバブケース（例: `maze-runner`, `quiz-battle`）
  - ユーザーが日本語で名前を言った場合は、適切な英語名に変換する
- **基本はシングルHTML**（CSS・JSはすべてインライン）で作る
- ゲームが大きくなってきたら、同じフォルダ内に `style.css` や `game.js` を分離してOK
- 画像素材が必要な場合は `assets/` フォルダにまとめる
- 各ゲームのHTMLには以下を必ず含める：
  - `<meta charset="UTF-8">` と `<meta name="viewport" ...>` （スマホでも遊べるように）
  - ページ上部またはゲームオーバー画面に「🏠 ゲームいちらんにもどる」リンク（`../../index.html` へ）
  - `<title>` にゲーム名を日本語で設定

### ゲームデザインの方針
- UIは日本語
- タッチ操作にも対応する（スマホで友達に見せられるように）
- 楽しい演出を入れる（クリア時の紙吹雪、効果音、アニメーションなど）
- 色使いは明るく見やすく
- ゲームオーバーや失敗時も、ポジティブなメッセージにする（「ざんねん！もういっかい？」など）

### ポータルページの管理
- ゲームを新しく作ったら、ルートの `index.html`（ゲーム一覧ページ）にも自動でリンクを追加する
- 一覧には以下の情報を含める：
  - ゲーム名（日本語）
  - ひとこと説明
  - リンク（`games/ゲーム名/index.html`）
- 一覧は新しいゲームが上に来るようにする

## コマンド対応表

ユーザーが以下のように言ったら、対応するアクションを実行してください：

| ユーザーの言葉 | アクション |
|---|---|
| 「見せて」「開いて」「プレビュー」 | `open games/ゲーム名/index.html` でブラウザを開く |
| 「公開して」「アップして」「みんなに見せたい」 | `git add -A && git commit -m "メッセージ" && git push` を実行 |
| 「一覧を見せて」「トップを見せて」 | `open index.html` でポータルページを開く |
| 「ゲームの名前を変えて」 | フォルダ名とポータルの表示名を両方変更する |
| 「消して」「削除して」 | 確認してから、ゲームフォルダとポータルのリンクを両方削除する |

### git commit メッセージの規則
- 新規ゲーム作成: `🎮 新しいゲーム: ゲーム名`
- ゲーム更新: `✨ ゲーム名を更新`
- ポータル更新: `📋 ゲーム一覧を更新`
- 削除: `🗑️ ゲーム名を削除`

## プロジェクト構成

```
kt-games/
├── CLAUDE.md          # この指示書
├── index.html         # ゲーム一覧ポータル
└── games/
    └── (ここにゲームフォルダが増えていく)
```

## GitHub Pages

- このリポジトリは GitHub Pages で公開されています
- main ブランチにpushすると、数十秒〜1分ほどで自動的にサイトに反映されます
- 公開URL: https://mizkun.github.io/kt-games/
- 公開完了後、「もう見れるよ！」とユーザーに伝えてあげてください

## 安全上の注意

- このプロジェクトのフォルダ（`~/kt-games/`）の外のファイルには触らない
- 個人情報（本名、住所、学校名など）をゲーム内やコミットメッセージに含めない
- 外部APIの呼び出しは行わない（HTMLとブラウザ標準APIのみで完結させる）
